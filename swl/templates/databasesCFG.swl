/**
 * Example:
 * Note: if your password haves the ':', '@', '\', and/or '/' characters, use the '\'
 * (backslash) before to escape it
database env#development# 'example' -> 'mysql://user:password@localhost:3306/schema' as default
 *        ^opt environment    ^alias    ^driver  ^user  ^pass   ^host   ^port ^schema   ^default connection
 */